<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="9dp"
    android:layout_margin="5dp"
    android:layout_alignParentEnd="true">

    <TextView
        android:id="@+id/tvRightTextMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        tools:text="there is some text"
        android:textSize="@dimen/textSizeChat"
        android:padding="8dp"
        android:background="@drawable/background_right"
        android:textColor="@color/primary_text"/>

    <ImageView
        android:id="@+id/ivRightImageMessage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentEnd="true"
        android:visibility="gone"
        android:contentDescription="@string/cd_image_sent" />

    <TextView
        android:id="@+id/tvRightIsTextSeen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        tools:text="seen"
        android:layout_below="@id/tvRightTextMessage"/>

</RelativeLayout>